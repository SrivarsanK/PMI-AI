# Plan 03-01: LSTM Model Interface

## Objective
Define the LSTM model architecture in PyTorch and implement a robust model loader.

## Frontmatter
- Wave: 1
- Depends on: 02-03
- Files modified: `requirements.txt`, `src/models/lstm.py`
- Autonomous: true
- Requirements addressed: [REQ-03]

## Tasks

<task>
<action>
Add `torch` and `torchvision` (optional) to `requirements.txt`.
</action>
<read_first>
- `requirements.txt`
</read_first>
<acceptance_criteria>
- `requirements.txt` contains `torch`.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/models/lstm.py` with an `RULPredictorLSTM` class inheriting from `torch.nn.Module`.
Structure: 
1. `nn.LSTM` (Hidden: 128, Layers: 2).
2. `nn.Linear` (Output: 1).
3. `forward` method for sequence processing.
</action>
<read_first>
- `.planning/phases/03-deep-learning-rul-estimation/03-RESEARCH.md`
- `.planning/phases/03-deep-learning-rul-estimation/03-CONTEXT.md`
</read_first>
<acceptance_criteria>
- `src/models/lstm.py` defines the LSTM architecture.
- Modular `input_dim` and `hidden_dim` parameters.
</acceptance_criteria>
</task>

<task>
<action>
Implement a model factory in `src/models/lstm.py` to load pre-trained weights from a `.pt` file if provided, otherwise initialize with default weights.
</action>
<read_first>
- `src/models/lstm.py`
</read_first>
<acceptance_criteria>
- Factory method `load_model(path)` exists.
</acceptance_criteria>
</task>

## Verification Criteria
1. `RULPredictorLSTM` can perform a dummy forward pass with a `torch.randn` tensor.
2. `torch` is imported without errors.
3. Model output shape is (batch, 1).

## Must Haves
- [ ] PyTorch integrated.
- [ ] Working LSTM model definition.
- [ ] Model loading interface.
