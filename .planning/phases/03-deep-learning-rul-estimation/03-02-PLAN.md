# Plan 03-02: RUL Estimation Service

## Objective
Implement the inference logic for RUL estimation, including sliding window normalization.

## Frontmatter
- Wave: 1
- Depends on: 03-01
- Files modified: `src/services/deep_learning_service.py`
- Autonomous: true
- Requirements addressed: [REQ-03]

## Tasks

<task>
<action>
Create `src/services/deep_learning_service.py` with a `RULService` class.
Initialize the RUL model on startup.
Implement a `prepare_input(window)` method that normalizes a sensor window using Min-Max scaling.
</action>
<read_first>
- `src/models/lstm.py`
- `.planning/phases/03-deep-learning-rul-estimation/03-CONTEXT.md`
</read_first>
<acceptance_criteria>
- `RULService` is initialized with the LSTM model.
- `prepare_input` converts raw windows to normalized `torch.Tensor`.
</acceptance_criteria>
</task>

<task>
<action>
Implement `estimate_rul(window)` method.
1. Calls `prepare_input`.
2. Runs model inference in `model.eval()` mode with `torch.no_grad()`.
3. Clips the result to a plausible range (e.g., 0 to 130).
</action>
<read_first>
- `src/services/deep_learning_service.py`
</read_first>
<acceptance_criteria>
- `estimate_rul` returns a rounded float for remaining life.
</acceptance_criteria>
</task>

## Verification Criteria
1. `RULService` produces consistent estimates for constant inputs.
2. Inference is fast (<50ms for single prediction).
3. Clipping logic prevents negative RUL values.

## Must Haves
- [ ] Deep Learning Service implemented.
- [ ] Normalization logic.
- [ ] Safe-inference wrapper (no_grad, eval mode).
