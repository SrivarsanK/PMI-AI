# Plan 03-03: Real-time RUL Integration

## Objective
Integrate the RUL estimation service into the async background pipeline using a ThreadPoolExecutor.

## Frontmatter
- Wave: 2
- Depends on: 03-02
- Files modified: `src/core/tasks.py`, `src/main.py`
- Autonomous: true
- Requirements addressed: [REQ-03]

## Tasks

<task>
<action>
Initialize a `ThreadPoolExecutor` in `src/main.py` state or as a global in `src/core/tasks.py` for CPU-bound RUL inference.
</action>
<read_first>
- `src/main.py`
</read_first>
<acceptance_criteria>
- `ThreadPoolExecutor` initialized.
</acceptance_criteria>
</task>

<task>
<action>
Update `process_telemetry_task` in `src/core/tasks.py` to call `RULService.estimate_rul` using `loop.run_in_executor`.
Ensure a window of size 30 is passed from the detector buffer.
</action>
<read_first>
- `src/core/tasks.py`
- `src/services/anomaly_service.py`
</read_first>
<acceptance_criteria>
- RUL prediction is run in a separate thread to avoid event loop lag.
- Results are logged in real-time.
</acceptance_criteria>
</task>

## Verification Criteria
1. Application logs show "Estimated RUL: X cycles" in background.
2. FastAPI `/health` endpoint remains responsive during inference spikes.
3. RUL updates on every valid ingestion where a 30-sample window is available.

## Must Haves
- [ ] Multi-threaded inference configuration.
- [ ] End-to-end integration: Ingest -> Filter -> Anomaly -> RUL.
