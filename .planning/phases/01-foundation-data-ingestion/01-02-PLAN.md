# Plan 01-02: Data Storage Service

## Objective
Integrate InfluxDB 2.x asynchronous client and establish the telemetry storage structure (Bucket/Org/Token).

## Frontmatter
- Wave: 2
- Depends on: 01-01
- Files modified: `requirements.txt`, `src/core/config.py`, `src/services/influx_service.py`
- Autonomous: true
- Requirements addressed: [REQ-01]

## Tasks

<task>
<action>
Add `influxdb-client[async]` to `requirements.txt`.
</action>
<read_first>
- `requirements.txt`
</read_first>
<acceptance_criteria>
- `requirements.txt` contains `influxdb-client[async]`.
</acceptance_criteria>
</task>

<task>
<action>
Update `src/core/config.py` to include InfluxDB settings: `INFLUXDB_URL`, `INFLUXDB_TOKEN`, `INFLUXDB_ORG`, `INFLUXDB_BUCKET`.
Update `.env.example` accordingly.
</action>
<read_first>
- `src/core/config.py`
- `.env.example`
</read_first>
<acceptance_criteria>
- `src/core/config.py` has `INFLUXDB_BUCKET` field.
- `.env.example` lists InfluxDB parameters.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/services/influx_service.py` with an `InfluxDBService` class.
Implement an asynchronous write method that takes a machine ID and sensor value.
Use `InfluxDBClientAsync`.
</action>
<read_first>
- `src/core/config.py`
- `.planning/research/01-RESEARCH.md`
</read_first>
<acceptance_criteria>
- `src/services/influx_service.py` imports `InfluxDBClientAsync`.
- `InfluxDBService.write_point` uses `Point("telemetry").tag("machine_id", ...)`.
</acceptance_criteria>
</task>

## Verification Criteria
1. `InfluxDBService` can be instantiated without errors.
2. Code includes `async with client` block for writing data.
3. Measurement is set to `telemetry` as per research.

## Must Haves
- [ ] InfluxDB 2.x client configured.
- [ ] Async write service implemented.
- [ ] Correct tagging schema (`machine_id`, `sensor_type`).
