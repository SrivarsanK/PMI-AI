# Plan 01-03: Ingestion API

## Objective
Expose the telemetry ingestion endpoints with strict Pydantic validation and API key protection.

## Frontmatter
- Wave: 3
- Depends on: 01-02
- Files modified: `src/main.py`, `src/api/routes.py`, `src/models/telemetry.py`
- Autonomous: true
- Requirements addressed: [REQ-01], [REQ-05]

## Tasks

<task>
<action>
Create `src/models/telemetry.py` with a `TelemetryIn` Pydantic model.
Fields: `machine_id` (str), `sensor_type` (str), `value` (float), `timestamp` (optional datetime).
Add validators to ensure `sensor_type` is one of: `temperature`, `vibration`, `pressure`.
</action>
<read_first>
- `.planning/research/01-RESEARCH.md`
</read_first>
<acceptance_criteria>
- `src/models/telemetry.py` exists with `TelemetryIn`.
- Field `sensor_type` has validation for specific strings.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/api/routes.py` with a `POST /ingest` endpoint.
The endpoint must:
1. Use `verify_api_key` dependency.
2. Accept `TelemetryIn` body.
3. Call `InfluxDBService.write_point` to persist data.
4. Return a 201 Created status.
</action>
<read_first>
- `src/core/security.py`
- `src/services/influx_service.py`
- `src/models/telemetry.py`
</read_first>
<acceptance_criteria>
- `src/api/routes.py` defines `@router.post("/ingest")`.
- Route uses `Security(verify_api_key)`.
- Route calls `influx_service.write_point`.
</acceptance_criteria>
</task>

<task>
<action>
Register the router in `src/main.py`.
Enable OpenAPI documentation tags for the Ingestion API.
</action>
<read_first>
- `src/main.py`
- `src/api/routes.py`
</read_first>
<acceptance_criteria>
- `src/main.py` contains `app.include_router(...)`.
- `/docs` shows the `/ingest` endpoint.
</acceptance_criteria>
</task>

## Verification Criteria
1. `curl -X POST /ingest` without API key returns 403.
2. `curl -X POST /ingest` with valid key and body returns 201.
3. Invalid `sensor_type` returns 422 Unprocessable Entity.

## Must Haves
- [ ] Protected ingestion endpoint.
- [ ] Strict type-based validation.
- [ ] End-to-end data flow from POST to Service.
