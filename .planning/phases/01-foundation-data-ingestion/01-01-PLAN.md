# Plan 01-01: Backend Foundation

## Objective
Set up the core FastAPI application with secure API key authentication and configuration management using Pydantic Settings.

## Frontmatter
- Wave: 1
- Depends on: Nothing
- Files modified: `requirements.txt`, `.env.example`, `src/main.py`, `src/core/config.py`, `src/core/security.py`
- Autonomous: true
- Requirements addressed: [REQ-05]

## Tasks

<task>
<action>
Initialize `requirements.txt` with core FastAPI and security dependencies.
Include: `fastapi`, `uvicorn`, `pydantic-settings`, `python-dotenv`.
</action>
<read_first>
- `requirements.txt` (if exists)
- `.planning/research/STACK.md`
</read_first>
<acceptance_criteria>
- `requirements.txt` exists and contains `fastapi`.
- `requirements.txt` contains `pydantic-settings`.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/core/config.py` using `BaseSettings` from `pydantic-settings`.
Define: `PROJECT_NAME`, `API_KEY` (secret string), and `DEBUG` mode.
Create `.env.example` with these keys.
</action>
<read_first>
- `.planning/research/STACK.md`
</read_first>
<acceptance_criteria>
- `src/core/config.py` contains `class Settings(BaseSettings)`.
- `.env.example` contains `API_KEY=`.
</acceptance_criteria>
</task>

<task>
<action>
Implement `src/core/security.py` with an `APIKeyHeader` dependency using `X-API-Key`.
The dependency must validate the key against settings.
</action>
<read_first>
- `src/core/config.py`
- `.planning/research/01-RESEARCH.md`
</read_first>
<acceptance_criteria>
- `src/core/security.py` defines `verify_api_key`.
- `verify_api_key` raises `HTTPException(403)` on invalid keys.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/main.py` entry point.
Initialize `FastAPI` app and add a health check endpoint.
</action>
<read_first>
- `src/core/config.py`
</read_first>
<acceptance_criteria>
- `src/main.py` creates `app = FastAPI()`.
- `app.get("/health")` exists and returns 200.
</acceptance_criteria>
</task>

## Verification Criteria
1. `uvicorn src.main:app` starts without errors.
2. `GET /health` returns JSON response.
3. `X-API-Key` validation logic is present in `security.py`.

## Must Haves
- [ ] FastAPI instance initialized.
- [ ] Pydantic Settings for API_KEY.
- [ ] Working health check endpoint.
