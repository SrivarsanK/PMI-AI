# Plan 02-01: Signal Processing Module

## Objective
Implement digital signal processing (DSP) utilities using SciPy to clean high-frequency sensor telemetry.

## Frontmatter
- Wave: 1
- Depends on: 01-03
- Files modified: `requirements.txt`, `src/services/processing_service.py`
- Autonomous: true
- Requirements addressed: [REQ-02]

## Tasks

<task>
<action>
Add `scipy` and `numpy` to `requirements.txt`.
</action>
<read_first>
- `requirements.txt`
</read_first>
<acceptance_criteria>
- `requirements.txt` contains `scipy`.
- `requirements.txt` contains `numpy`.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/services/processing_service.py` with a `SignalProcessor` class.
Implement `apply_butterworth_filter` using `scipy.signal.butter` (sos format).
Default order: 2.
Default cutoff: fs/10.
</action>
<read_first>
- `.planning/phases/02-signal-processing-anomaly-detection/02-RESEARCH.md`
- `.planning/phases/02-signal-processing-anomaly-detection/02-CONTEXT.md`
</read_first>
<acceptance_criteria>
- `src/services/processing_service.py` defines `apply_butterworth_filter`.
- Filter uses `sosfilt` or `sosfiltfilt`.
- `from scipy import signal` is present.
</acceptance_criteria>
</task>

<task>
<action>
Implement `extract_features` method in `SignalProcessor`.
Calculates `rolling_mean` and `rolling_std` of a signal window.
</action>
<read_first>
- `src/services/processing_service.py`
</read_first>
<acceptance_criteria>
- `extract_features` takes a `numpy` array and returns a feature vector.
</acceptance_criteria>
</task>

## Verification Criteria
1. `SignalProcessor` can process a dummy numpy array without errors.
2. Filter return values are smoothed compared to raw noisy input.
3. Features match expected mean/std of the window.

## Must Haves
- [ ] SciPy/NumPy integrated.
- [ ] Butterworth filtering implemented.
- [ ] Modular feature extraction.
