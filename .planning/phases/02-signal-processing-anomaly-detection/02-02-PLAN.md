# Plan 02-02: Anomaly Detection Component

## Objective
Implement the unsupervised anomaly detection logic using the Isolation Forest algorithm.

## Frontmatter
- Wave: 1
- Depends on: 02-01
- Files modified: `requirements.txt`, `src/services/anomaly_service.py`
- Autonomous: true
- Requirements addressed: [REQ-02]

## Tasks

<task>
<action>
Add `scikit-learn` to `requirements.txt`.
</action>
<read_first>
- `requirements.txt`
</read_first>
<acceptance_criteria>
- `requirements.txt` contains `scikit-learn`.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/services/anomaly_service.py` with an `AnomalyDetector` class.
Initialize `IsolationForest` (n_estimators=100, contamination=0.05).
Implement `detect(features)` method.
Provide a `train(data)` method to establish a baseline model.
</action>
<read_first>
- `.planning/phases/02-signal-processing-anomaly-detection/02-RESEARCH.md`
</read_first>
<acceptance_criteria>
- `src/services/anomaly_service.py` imports `IsolationForest`.
- Detector returns a score or label (True/False for anomaly).
</acceptance_criteria>
</task>

<task>
<action>
Initialize a global state for windows in `src/services/anomaly_service.py`.
Store a limited buffer (e.g., 100 points) of filtered readings per `machine_id`.
</action>
<read_first>
- `src/services/anomaly_service.py`
</read_first>
<acceptance_criteria>
- Buffer mechanism stores temporal data by machine key.
</acceptance_criteria>
</task>

## Verification Criteria
1. Model can be trained on synthetic "normal" data.
2. Outlier points correctly receive higher anomaly scores or "anomaly" labels.
3. Cache/buffer remains within memory bounds.

## Must Haves
- [ ] Scikit-learn integrated.
- [ ] Isolation Forest logic implemented.
- [ ] Buffer system for rolling windows.
