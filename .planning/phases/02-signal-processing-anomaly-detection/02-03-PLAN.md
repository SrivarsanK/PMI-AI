# Plan 02-03: Integration & Async Pipeline

## Objective
Connect signal processing and anomaly detection to the main ingestion flow using background tasks.

## Frontmatter
- Wave: 2
- Depends on: 02-02
- Files modified: `src/api/routes.py`, `src/core/tasks.py`
- Autonomous: true
- Requirements addressed: [REQ-02]

## Tasks

<task>
<action>
Create `src/core/tasks.py` for background processing jobs.
Implement `process_telemetry_task(machine_id, value)`.
This task:
1. Updates the machine's buffer.
2. Applies the noise filter.
3. Extracts features.
4. Detects anomaly status.
5. Logs results if an anomaly is detected.
</action>
<read_first>
- `src/services/processing_service.py`
- `src/services/anomaly_service.py`
</read_first>
<acceptance_criteria>
- `src/core/tasks.py` coordinates processing and detection.
- Task is asynchronous.
</acceptance_criteria>
</task>

<task>
<action>
Update `src/api/routes.py` to trigger `process_telemetry_task` as a `BackgroundTasks` after DB write.
Ensure FastAPI's `BackgroundTasks` dependency is injected into the route handler.
</action>
<read_first>
- `src/api/routes.py`
- `src/core/tasks.py`
</read_first>
<acceptance_criteria>
- `@router.post("/ingest")` accepts `BackgroundTasks`.
- `background_tasks.add_task(process_telemetry_task, ...)` is called.
</acceptance_criteria>
</task>

## Verification Criteria
1. API ingestion still succeeds quickly (as processing is offloaded to background).
2. Logs show "Anomaly detected" for abnormal values during testing.
3. Data flows from Route -> Service -> Background Task.

## Must Haves
- [ ] Functioning background processing pipeline.
- [ ] Real-time anomaly detection triggered on every point.
