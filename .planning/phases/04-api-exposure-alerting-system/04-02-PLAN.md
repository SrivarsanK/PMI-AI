# Plan 04-02: Dashboard REST API

## Objective
Implement REST endpoints to serve machine lists and current health status for the web dashboard.

## Frontmatter
- Wave: 1
- Depends on: 03-03
- Files modified: `src/api/routes.py`, `src/models/health.py`
- Autonomous: true
- Requirements addressed: [REQ-04]

## Tasks

<task>
<action>
Create `src/models/health.py` with pydantic models: `MachineHealthSummary` (id, status, last_rul, last_update).
</action>
<read_first>
- `.planning/phases/04-api-exposure-alerting-system/04-CONTEXT.md`
</read_first>
<acceptance_criteria>
- Pydantic models for health reporting exist.
</acceptance_criteria>
</task>

<task>
<action>
Implement `GET /machines` to return a summary of all active machines in `src/api/routes.py`.
Initially use the internal anomaly buffer/RUL cache to derive this list.
</action>
<read_first>
- `src/api/routes.py`
- `src/core/tasks.py`
</read_first>
<acceptance_criteria>
- Route exists and is protected by API key.
- Returns list of machine health summaries.
</acceptance_criteria>
</task>

<task>
<action>
Implement `GET /machines/{id}/health` for detailed history (filtered signal trend).
Query InfluxDB for the last 100 points.
</action>
<read_first>
- `src/services/influx_service.py`
</read_first>
<acceptance_criteria>
- Route returns time-series health data.
</acceptance_criteria>
</task>

## Verification Criteria
1. `GET /machines` returns a valid JSON list.
2. Machine status matches the logic (Healthy, Warning, Critical).
3. Data is correctly serialized via Pydantic.

## Must Haves
- [ ] REST health endpoints.
- [ ] Health status logic.
- [ ] Pydantic response models.
