# Plan 04-01: WebSocket Connection Manager

## Objective
Implement WebSocket handling to broadcast real-time machine health and alert notifications.

## Frontmatter
- Wave: 1
- Depends on: 03-03
- Files modified: `src/core/connection_manager.py`, `src/api/websockets.py`, `src/main.py`
- Autonomous: true
- Requirements addressed: [REQ-04]

## Tasks

<task>
<action>
Create `src/core/connection_manager.py` with a `ConnectionManager` class.
Implement `handle_connect(websocket)`, `handle_disconnect(websocket)`, and `broadcast(message)`.
</action>
<read_first>
- `.planning/phases/04-api-exposure-alerting-system/04-RESEARCH.md`
</read_first>
<acceptance_criteria>
- `ConnectionManager` keeps track of active connections.
- `broadcast` iterates through the list of active websockets.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/api/websockets.py` with the `/ws/alerts` endpoint.
Ensure basic API Key verification as query param.
</action>
<read_first>
- `src/core/connection_manager.py`
- `.planning/phases/04-api-exposure-alerting-system/04-CONTEXT.md`
</read_first>
<acceptance_criteria>
- `@router.websocket("/ws/alerts")` route exists.
- Injects `ConnectionManager` (as singleton).
</acceptance_criteria>
</task>

<task>
<action>
Include the WebSocket router in `src/main.py`.
</action>
<read_first>
- `src/main.py`
</read_first>
<acceptance_criteria>
- Application includes the WebSocket router.
</acceptance_criteria>
</task>

## Verification Criteria
1. Client can connect to `/ws/alerts?api_key=...`.
2. Disconnected clients are correctly removed from the internal list.
3. Errors in one connection do not kill others.

## Must Haves
- [ ] Connection manager logic.
- [ ] WebSocket endpoint implemented.
- [ ] Basic query-param auth.
