# Plan 04-03: Real-time Alert Broadcasting

## Objective
Connect background detection tasks to the WebSocket broadcasting mechanism to push alerts to clients immediately.

## Frontmatter
- Wave: 2
- Depends on: 04-01
- Files modified: `src/core/tasks.py`
- Autonomous: true
- Requirements addressed: [REQ-04]

## Tasks

<task>
<action>
Update `process_telemetry_task` in `src/core/tasks.py` to call `ConnectionManager.broadcast()` when:
- An anomaly is detected.
- RUL estimate is < 20.0 (Critical status).
Send a JSON payload with the machine id and the alert details.
</action>
<read_first>
- `src/core/tasks.py`
- `src/core/connection_manager.py`
</read_first>
<acceptance_criteria>
- Critical events are broadcast to WebSockets.
- Payload is JSON formatted.
</acceptance_criteria>
</task>

## Verification Criteria
1. Active WebSocket clients receive an alert JSON as soon as an anomaly is triggered.
2. No lag between detection in background and broadcast to frontend.
3. System handles broadcasting to zero active clients without errors.

## Must Haves
- [ ] Task -> WebSocket bridge.
- [ ] Critical alert broadcasting.
