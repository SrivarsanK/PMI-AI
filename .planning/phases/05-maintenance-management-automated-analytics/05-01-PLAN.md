# Plan 05-01: Maintenance Priority API

## Objective
Implement ranking and prioritization logic for maintenance tasks based on RUL and anomalies.

## Frontmatter
- Wave: 1
- Depends on: 04-02
- Files modified: `src/models/health.py`, `src/services/maintenance_service.py`, `src/api/routes.py`
- Autonomous: true
- Requirements addressed: [REQ-04], [REQ-06]

## Tasks

<task>
<action>
Create `src/models/maintenance.py` with `MaintenancePrio` (machine_id, score, urgency_level, last_anomalies).
Update `src/models/health.py` if needed.
</action>
<read_first>
- `.planning/phases/05-maintenance-management-automated-analytics/05-CONTEXT.md`
</read_first>
<acceptance_criteria>
- Pydantic models for ranking exist.
</acceptance_criteria>
</task>

<task>
<action>
Create `src/services/maintenance_service.py` with a `MaintenanceManager`.
Implement `get_maintenance_ranking()` that aggregates health metadata from recent buffers/InfluxDB and sorts by RUL.
Break ties using anomaly count.
</action>
<read_first>
- `src/services/anomaly_service.py`
- `src/services/deep_learning_service.py`
</read_first>
<acceptance_criteria>
- Ranking logic correctly identifies the most critical machines first.
- Ties are handled using secondary metrics.
</acceptance_criteria>
</task>

<task>
<action>
Expose `GET /maintenance/schedule` in `src/api/routes.py` linked to `MaintenanceManager.get_maintenance_ranking()`.
</action>
<read_first>
- `src/api/routes.py`
</read_first>
<acceptance_criteria>
- Route returns a prioritized list.
</acceptance_criteria>
</task>

## Verification Criteria
1. `GET /maintenance/schedule` returns machines in descending order of criticality (low RUL first).
2. Urgency labels (EMERGENCY, URGENT, RECOMMENDED) are applied correctly based on thresholds.
3. List is empty if no telemetry has been processed.

## Must Haves
- [ ] Priortized ranking logic.
- [ ] Maintenance schedule endpoint.
- [ ] Urgency classification levels.
