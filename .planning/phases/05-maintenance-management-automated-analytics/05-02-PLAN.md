# Plan 05-02: Analytics & Reporting

## Objective
Implement MTBF calculations and reliability trend analytics to provide higher-level insights for industrial management.

## Frontmatter
- Wave: 2
- Depends on: 05-01
- Files modified: `src/services/maintenance_service.py`, `src/api/routes.py`, `src/services/influx_service.py`
- Autonomous: true
- Requirements addressed: [REQ-07]

## Tasks

<task>
<action>
Implement basic `MTBF()` calculation in `src/services/maintenance_service.py`.
Initially use a mock or a small historical interval from InfluxDB.
</action>
<read_first>
- `src/services/influx_service.py`
- `.planning/phases/05-maintenance-management-automated-analytics/05-RESEARCH.md`
</read_first>
<acceptance_criteria>
- Function returns MTBF for a given machine id.
</acceptance_criteria>
</task>

<task>
<action>
Implement `GET /analytics/mtbf` and `GET /analytics/reliability` for all machines.
Add simple caching for these calculated values (they shouldn't be recalculated for every request).
</action>
<read_first>
- `src/api/routes.py`
</read_first>
<acceptance_criteria>
- Analytics endpoints exist.
- Response payloads have historical metrics.
</acceptance_criteria>
</task>

## Verification Criteria
1. `GET /analytics/mtbf` returns a numeric value for known machines.
2. Metrics are updated periodically (or via a "Refresh" call).
3. System handles zero-history machines by returning "Insufficient Data".

## Must Haves
- [ ] MTBF calculation.
- [ ] Analytics REST routes.
